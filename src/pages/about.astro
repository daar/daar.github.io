---
import { Image } from "astro:assets";
import Layout from "../layouts/Layout.astro";

import aboutImage from "../assets/wp006.jpg";
---

<style>
  /* Smooth Fade-In Transition */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.7s ease-out,
      transform 0.7s ease-out;
  }

  .fade-in-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Column Flexbox Setup */
  .column {
    flex: 1;
  }

  /* Middle Column Styling */
  .middle-column {
    max-width: 61.8%;
  }

  @media (max-width: 640px) {
    .middle-column {
      max-width: 100%;
    }
  }

  /* Justify Content and Align Text */
  .bio {
    text-align: justify;
    line-height: 1.75;
  }

  /* Image Styling - Force Full Width, Fixed Height */
  .image {
    width: 100%;
    height: 30vh;  /* 30% of viewport height */
    object-fit: cover; /* Crops to fit, prevents distortion */
    border-radius: 12px;
    display: block;  /* Prevents unwanted inline spacing */
    margin-bottom: 1.5rem;
  }
</style>

<Layout>
  <main class="flex justify-center min-h-screen py-8">
    <!-- Left Column (empty on larger screens, hidden on mobile) -->
    <div class="column golden-column hidden sm:block"></div>

    <!-- Middle Column (image and text) -->
    <div
      class="column middle-column content fade-in mx-auto transition-all ease-in-out transform duration-700 pt-16"
    >
      <!-- Image at the top -->
      <Image
        src={aboutImage}
        alt="About me"
        class="image"
      />

      <!-- Text content -->
      <p class="bio text-lg text-justify text-gray-700 leading-relaxed">
        From a young age, I've been captivated by the beauty of sunlight. For
        me, capturing its essence with silver is a personal form of expression.
        Over the years, I’ve explored various techniques in search of the purest
        way to convey light, from 35mm to pinhole photography. Today, I work
        with wet plate collodion on 8x10 plates, where the light captured by my
        camera becomes the final, tangible result.
      </p>
      <p class="bio text-lg text-justify text-gray-700 leading-relaxed">
        ~Darius~
      </p>
    </div>

    <!-- Right Column (empty on larger screens, hidden on mobile) -->
    <div class="column golden-column hidden sm:block"></div>
  </main>

  <script is:inline>
    document.addEventListener("DOMContentLoaded", function () {
      const contentElement = document.querySelector(".content");
      contentElement.classList.add("fade-in-visible");
    });
  </script>
</Layout>
